# Forward task metadata — matches the master Everglades CLI structure.
# Keep values clean (no inline comments) for safe YAML parsing.

domain: mechanical_engineering
sub_domain: fem_analysis
direction: forward
simulator: scikit-fem
timeout_minutes: 30

# Skill-internal fields:
name: <draft-name>

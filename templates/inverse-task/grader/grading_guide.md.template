# Grading Guidance

<!-- Canonical Everglades CLI location: grader/grading_guide.md.
     Maps to the RLS "Grading Guidance" custom field on push.
     The expert OWNS this file. The skill must NOT write it. -->

## Correct answer

<!-- Paste the golden answer. Match the format your prompt asks for. -->

## Acceptance criteria

<!-- Tolerance, units, format rules. e.g., "Values within 5% relative tolerance.
     Exact case-insensitive string match for the topology label." -->

## Common near-misses

<!-- One row per plausible wrong answer. Per Playbook Step 3: each row gets a
     1-sentence explanation of WHY it loses. -->

| Submitted | Why it loses |
|---|---|
| `<wrong-value-1>` | <one sentence — the naive strategy that produces this> |
| `<wrong-value-2>` | <one sentence — convention-default failure> |
| `<wrong-value-3>` | <one sentence — partial credit / formatting variant> |

## Edge cases the judge should know

<!-- e.g., "If the model submits the answer in a JSON wrapper, accept iff the
     value is unambiguous. Reject if the model returns multiple candidates." -->

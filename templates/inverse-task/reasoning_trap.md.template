# Reasoning Trap

<!-- The naive textbook approach that silently gives a plausible but wrong
     answer. The expert OWNS this file. This is shared with the client. -->

## The trap

<!-- One paragraph: what naive approach does the model retrieve from training,
     and why does it silently produce a wrong answer? -->

## Why a careful solver succeeds

<!-- One paragraph: what reasoning step does the careful solver take that the
     naive solver skips? -->
